fn Create = do
	batch <- new Batch2D;
end

fn Resize(width, height) = do
	batch.Resize(width, height);
end

fn Render = do
	batch.Begin();
	batch.Rect(10, 10, 50, 50);
	batch.End();
end

fn Dispose = do
	batch.Dispose();
end